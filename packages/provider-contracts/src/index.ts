/**
 * Reserved for future payment-provider interfaces (e.g. a PaymentProvider contract).
 * Intentionally exports only a package identifier for now.
 */
export const PROVIDER_CONTRACTS_PACKAGE_NAME = '@fraterunion-payments/provider-contracts' as const;
