/**
 * Reserved for future environment and application configuration utilities.
 * Intentionally exports only a package identifier for now.
 */
export const CONFIG_PACKAGE_NAME = '@fraterunion-payments/config' as const;
