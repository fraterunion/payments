/** Machine-readable service identifier, used in health check responses. */
export const SERVICE_ID = 'fraterunion-payments-api';

/** Human-readable product name, used in the versioned API root response. */
export const PRODUCT_NAME = 'FraterUnion Payments API';
